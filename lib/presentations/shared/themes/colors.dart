import 'package:flutter/material.dart';

const mobileBackgroundColor = Color(0xFFF8F8F8);
const barBackgroundColor = Colors.white70;
const appBarBackgroundColor = Color(0x00FFFFFF);
const primaryColor = Color(0xFFCBE7B5);
const primaryLightColor = Color(0xFFEDF8F0);
const tabBarBackground = Color(0xFFEDF8F0);
var mainColorScheme = ColorScheme.fromSeed(
    seedColor: primaryColor, primaryContainer: primaryColor);
